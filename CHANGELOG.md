# Changelog

## 1.26.818 - 2026-08-18

- 修复浏览器回归测试的已登录复用、大小写同名文件定位和分享目录预览控件断言。
- 分享创建、管理和预览中的复制链接均优先写入 Clipboard API；失败时会尝试旧浏览器复制，并选中链接供手动复制。
- 分享目录复用原目录的图片、Markdown、PDF 和文本预览弹窗；HTML 保持受控沙箱新窗口入口，下载权限不变。
- 补充桌面与移动端复制降级，以及分享目录预览、HTML 和下载的浏览器回归测试。
- 分享入口、无效链接和目录浏览改用资料架统一布局；目录内资源继续要求当前分享会话授权。
- 创建分享会在浏览器拦截少于 6 位的密码，并展示服务端返回的具体参数错误；空错误提示不再渲染。
- 页脚显示构建版本，补充分享错误、移动端布局与空状态浏览器回归测试。
- 目录页可创建当前目录分享，并在管理面板以“当前目录分享”展示、复制和删除。
- 受保护目录新增统一分享管理面板，可查看文件与文件夹分享的链接、密码要求、下载限制、到期时间和状态，并支持复制、查看与确认删除。
- 目录分享接口支持使用受控的当前目录标记创建和查询分享，不允许客户端指定父级或同级路径。
- 目录列表的类型排序改为按真实扩展名排序；目录、无扩展名文件和大小写不同的扩展名都有稳定顺序。
- 目录表头新增修改时间排序，四个排序字段均支持升降序，并在移动端保留可访问的排序入口。

## 1.26.817 - 2026-08-17

- 目录浏览新增可保持会话状态的名称、类型和大小排序；长目录提供固定表头与按溢出状态显示的平滑顶部/底部导航。
- 修复目录表头被滚动容器限制而失效的问题，并在移动端保留大小排序入口与键盘操作回归测试。
- 对齐移动端目录的名称、类型和大小列；操作移至下一行，避免大小表头与内容脱节。
- 在目录项操作区增加文件、文件夹受控分享表单与链接复制交互；创建请求仅提交服务端给出的当前目录直接子项。
- 增加受保护目录的分享创建接口、原子配置写入和目录树能力边界校验；目录分享遇到更严格的子目录密码时立即拒绝。
- 修复分享密码裸值被静默拒绝，以及小写 `password` 被错误记录为应用锁定的问题。
- 补齐第三方许可清单、演示 GIF 和 GitHub 仓库发布元数据。

## 1.26.813 - 2026-08-13

- 统一发布标签、Release 标题、四平台产物和 SHA256 校验清单使用 `v1.26.813`。

## 1.26.812 - 2026-08-12

- 限制无效分享能力令牌的资料树扫描频率，避免公共分享入口被连续猜测放大为磁盘遍历负载。
- 修复页面缺少键盘“跳到主要内容”入口，以及分享链接复制失败时没有可感知反馈的问题；补充相应的浏览器回归测试。
- 修复受保护目录在认证成功后遗漏配置说明的模板上下文，并覆盖实际鉴权目录路由的标题/说明回归测试。
- 修复密码页操作中心不一致及 HTML 文件名被源码预览拦截的问题；新增 Playwright Chromium 的 1440px、390px 与受控 HTML 路由回归测试。
- 修复全局 `SHARE_ENABLED` 被误解析为分享定义而触发崩溃的问题；复用统一键解析并覆盖 HTML、Markdown 分享认证与渲染回归。
- 修复继承密码的会话策略版本：空目录及仅含展示/分享字段的 `.env` 不再形成隐式会话边界；密码或 `html_scripts` 的有效祖先变更仍会立即要求重新认证。
- 仅在已授权目录向可用分享下发服务端生成的 `ShareURL`，供预览弹窗打开或复制链接；过期、无效和未授权响应不暴露分享能力凭据。
- 修复外置预览脚本的模板残留与图片“适应”双重缩放；密码页操作改为网格排布，目录提示区与正文分层，页脚内容居中。
- 实现请求级应用目录快照、三级 `.env` 密码与 `html_scripts` 继承、无前缀密码原子迁移，以及 HTML/Markdown 分享的受控直达渲染；目录中的 `index.html` 不再接管目录浏览。
- 增加内容指纹化 `/_assets/` CSS/JS 输出、强 ETag 与一年 immutable 缓存；页面和受保护内容维持 `no-store`。
- 受保护目录在上级列表恢复显示显式标题与说明，同时继续绝不输出密码、哈希、分享令牌或本地路径。
- 明确文件动态发现、`.env` 继承与无前缀密码迁移、HTML/分享隔离渲染、指纹静态资源缓存及前后端实施边界，作为 MT-192 后续实现与验收契约。
- 修复认证前目录元数据泄露：受保护或锁定应用在首页和登录页仅显示通用文案，并覆盖首页、应用根与深层目录的无 Cookie 回归测试。
- 收紧预览弹窗工具栏：PDF、文本、Markdown 与 HTML 源码不再保留底部区域；图片按钮按服务端缩放、导航与相邻 URL 能力逐项显示，并在 320px 下让右上角操作区安全换行。
- 完成 Explore 最终能力对接：HTML 以“预览源码”与受控视图双入口呈现，图片缩放/导航严格消费服务端能力字段，分享仅展示无凭据状态；将打开/下载操作移至弹窗右上角。
- 为目录项增加服务端生成的 `OpenKind`、图片相邻导航和无凭据分享状态契约；HTML 使用应用前缀下的受控渲染外壳与双重 sandbox，旧受控资源地址仅作语法重定向。
- 将资料浏览调整为 MT-166 Explore 结构：56px 顶栏、资料架入口与独立 40px 显示偏好菜单；补充目录说明、响应式列表和全页产品页脚。
- 修复受保护应用无法访问预览和下载端点：登录时为应用页、预览与下载前缀分别签发最小作用域的应用专属会话 Cookie，并覆盖真实 Cookie Jar 回归测试。
- 将主题、明暗与色系偏好收进右上角可键盘操作的菜单，并分别以本地存储记忆。
- 预览弹窗增加服务端能力驱动的新窗口/下载操作、图片缩放与拖拽；Markdown 仅在沙箱 iframe 中展示后端安全渲染结果。
- 根配置迁移为数据根 `.env`：默认使用启动目录，`-dir` 显式覆盖；旧 `datashelf.env` 被检测后 fail-closed 并提供迁移提示。
- 增加安全 Markdown 预览、受控新窗口 URL 与 `/_download` 下载端点；预览能力由服务端分类并在认证后执行。
- 收紧根/子 `.env` 的 UTF-8、大小、未知字段和符号链接校验，补充根密码会话失效与 Markdown 攻击输入回归测试。
- 移除页面 320px 的刚性最小宽度，避免纵向滚动条出现时产生小屏水平滚动。
- 迁移应用规范路由到 `/<slug>/`，为历史 `/a/<slug>/` 增加经路径校验的 GET/HEAD 308 兼容，并收紧保留命名空间与 Cookie Path。
- 首页改用规范应用地址；目录模板只消费服务端提供的预览分类、打开方式与预览 URL。
- 扩充路由、配置、密码策略与预览安全回归测试。
- 建立 DataShelf Go 1.25 单二进制服务骨架。
- 实现应用扫描、私有配置、Argon2id 密码迁移和 fail-closed 锁定。
- 实现应用级会话鉴权、登录限速、安全路径解析、目录浏览和流式文件服务。
- 增加访问控制、配置迁移、会话隔离和路径防护测试。
- 将首页、目录和密码页提取为 `go:embed` 内嵌模板，补充响应式布局、目录元数据、空状态与辅助技术提示。
- 增加资料架导航、可记忆的本地主题、可访问的文件预览弹窗与受控格式白名单。
